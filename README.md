# oddsloop status

Public status dashboard for **oddsloop**, an autonomous prediction-market
analysis loop (paper trading only — no real money is traded).

Live: https://peppajoke.github.io/oddsloop-status/

`stats.json` is pushed automatically by the (private) trading loop after each
cycle. This repo contains only the dashboard and its data — no strategy code.
